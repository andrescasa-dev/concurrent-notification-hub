name: thc-mentoria-notifications-test

services:
  postgres-test:
    image: postgres:16-alpine
    container_name: thc-postgres-test
    ports:
      - '5433:5432'
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: thc_test
